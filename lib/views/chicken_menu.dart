import 'package:flutter/material.dart';

class ChickenMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Chicken Section',style: TextStyle(fontSize: 22,fontWeight: FontWeight.bold),),
        
      ),
    );
  }
}