import 'package:flutter/material.dart';

class PizzaMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Pizza Section',style: TextStyle(fontSize: 22,fontWeight: FontWeight.bold),),
        
      ),
    );
  }
}