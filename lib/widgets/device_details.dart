import 'package:flutter/material.dart';
import 'dart:ui';

class DeviceDetails {
  static double deviceHeight = window.physicalSize.height;
  static double deviceWidth = window.physicalSize.width;

  // below for sidebar
  
  static double chickenTop = - (deviceHeight * 0.018 )/100;
  static double pizzaTop =-(deviceHeight * 0.005)/100;
  static double bergerTop =(deviceHeight * 0.010 )/100;
  static double coldDrinkTop =(deviceHeight * 0.024)/100;

  
}
